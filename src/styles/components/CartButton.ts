import { styled } from "..";

export const CartButtonContainer = styled('button', {
  padding: '0.75rem',
  backgroundColor: '$gray800',
  border: 0,
  borderRadius: 6,
  cursor: 'pointer',

  svg: {
    color: '$gray700',
  }
})